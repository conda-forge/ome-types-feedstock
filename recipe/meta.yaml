{% set name = "ome-types" %}
{% set version = "0.3.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/ome_types-{{ version }}.tar.gz
  sha256: b7b6e8f22d9c6a3b6e1bccd09c416fc8ba3dd476fb864c89469758b7bfff43b7

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - autoflake
    - black
    - isort >=5.0
    - numpydoc
    - pip
    - pydantic
    - python >=3.7
    - setuptools >=42
    - setuptools-scm
    - wheel
    - xmlschema ==1.4.1
  run:
    - lxml >=4.8.0
    - pint >=0.15
    - pydantic >=1.0
    - python >=3.7
    - xmlschema >=1.4.1,<2

test:
  imports:
    - ome_types
  commands:
    - pip list
    - pip check
  requires:
    - pip

about:
  home: https://github.com/tlambert03/ome-types
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: native python dataclasses for the OME data model
  doc_url: https://ome-types.readthedocs.io/en/latest/
  dev_url: https://github.com/tlambert03/ome-types

extra:
  recipe-maintainers:
    - tlambert03
